-- GatorSync initial database setup
-- Sprint 1 will add schema for users, roles, syllabi, schedules, etc.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Placeholder: tables will be added in subsequent sprints.
